import React from 'react';
import Title from '.';

const Template = () => <Title>title</Title>;

export const IntroTitle = Template.bind({});

export default {
  title: 'Atoms/title',
  component: IntroTitle,
};
